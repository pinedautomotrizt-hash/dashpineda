# Produccion:
#VITE_API_URL=https://reportepineda-production.up.railway.app/api

# Backend local (reactivar para pruebas locales):
# Usa 127.0.0.1 (NO "localhost") para que coincida con el host del frontend
# (vite --host 127.0.0.1) y las cookies de sesion viajen entre ambos. Si esto
# dice "localhost" el login parece funcionar pero te regresa solo al login.
VITE_API_URL=http://127.0.0.1:3001/api


